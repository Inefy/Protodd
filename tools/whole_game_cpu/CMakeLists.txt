cmake_minimum_required(VERSION 3.24)
project(WholeGameCpu LANGUAGES CXX)
add_executable(whole_game_cpu_probe main.cpp ../../src/cpu/WholeGameCpu.cpp
               ../../src/cpu/WholeGameEncoder.cpp)
target_include_directories(whole_game_cpu_probe PRIVATE ../../src/cpu ../../include)
target_compile_features(whole_game_cpu_probe PRIVATE cxx_std_20)
if(WIN32)
    add_executable(whole_game_resource_probe resource_probe.cpp ../../src/cpu/WholeGameCpu.cpp)
    target_include_directories(whole_game_resource_probe PRIVATE ../../src/cpu ../../include)
    target_compile_features(whole_game_resource_probe PRIVATE cxx_std_20)
endif()
if(MSVC)
    target_compile_options(whole_game_cpu_probe PRIVATE /W4 /O2 /arch:SSE2)
endif()
add_executable(whole_game_encoder_probe encoder_probe.cpp ../../src/cpu/WholeGameEncoder.cpp)
target_include_directories(whole_game_encoder_probe PRIVATE ../../src/cpu ../../include)
target_compile_features(whole_game_encoder_probe PRIVATE cxx_std_20)
if(MSVC)
    target_compile_options(whole_game_encoder_probe PRIVATE /W4 /O2)
endif()
add_executable(whole_game_intent_probe intent_probe.cpp ../../src/cpu/WholeGameIntent.cpp)
target_include_directories(whole_game_intent_probe PRIVATE ../../src/cpu ../../include)
target_compile_features(whole_game_intent_probe PRIVATE cxx_std_20)
if(MSVC)
    target_compile_options(whole_game_intent_probe PRIVATE /W4 /O2)
endif()
add_executable(whole_game_schedule_probe schedule_probe.cpp)
target_include_directories(whole_game_schedule_probe PRIVATE ../../src/cpu ../../include)
target_compile_features(whole_game_schedule_probe PRIVATE cxx_std_20)
if(MSVC)
    target_compile_options(whole_game_schedule_probe PRIVATE /W4 /O2)
endif()
